---
- name: Install PostgreSQL
  apt:
    name: postgrespro-1c-{{ postgresql_1c_version }}
    state: present
    update_cache: yes
  notify: 
    - Restart PostgreSQL
    - Remove useless packages
    - Remove useless dependencies

- name: Flushing handlers
  meta: flush_handlers
